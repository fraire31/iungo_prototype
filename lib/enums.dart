enum signInOptions { email, facebook, google, apple }
enum userType { user, provider }
enum serviceOptions { parts, services }
enum providerLocation { nearMe, outOfState }
enum providerView { requests, quotes }
